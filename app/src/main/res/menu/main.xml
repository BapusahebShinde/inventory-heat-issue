<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  android:layout_width="match_parent"
  android:layout_height="wrap_content">

  <item
    android:id="@+id/act_ser"
    android:icon="@drawable/ic_act_ser"
    android:orderInCategory="100"
    android:title="@string/action_ser"
    android:titleCondensed="@string/action_ser"
    android:tooltipText="@string/action_ser"
    app:showAsAction="always" />
  <item
    android:id="@+id/act_msg"
    android:icon="@drawable/ic_act_msg_disable"
    android:orderInCategory="100"
    android:title="@string/action_msg"
    android:titleCondensed="@string/action_msg"
    android:tooltipText="@string/action_msg"
    app:showAsAction="always" />
  <item
    android:id="@+id/act_notify"
    android:icon="@drawable/ic_act_notify"
    android:orderInCategory="100"
    android:title="@string/action_notify"
    android:titleCondensed="@string/action_notify"
    android:tooltipText="@string/action_notify"
    app:showAsAction="always" />

  <item
    android:id="@+id/act_logout"
    android:icon="@drawable/ic_act_logout"
    android:orderInCategory="100"
    android:title="@string/action_logout"
    android:titleCondensed="@string/action_logout"
    android:tooltipText="@string/action_logout"
    app:showAsAction="always" />

</menu>