package com.itek.retail.ui.customviews.photoview;

/**
 * The interface On view drag listener.
 */
public interface OnViewDragListener{
  
  /**
   * On drag.
   *
   * @param dx the dx
   * @param dy the dy
   */
  void onDrag(float dx, float dy);
}
